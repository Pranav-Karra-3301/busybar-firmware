Import("env")

for libname in [
    "bsb_protobuf",
]:
    # print(f"In {env.Real(Dir('.'))}, registering {libname}")
    env.RegisterComponent(f"fwlib_{libname}", env.FindComponentSconscript(libname))

